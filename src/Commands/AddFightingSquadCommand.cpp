#include "Commands/AddFightingSquadCommand.h"

void AddFightingSquadCommand::execute(Player& player, const std::vector<int>& args) const {
    //TODO: player.addSquad(deque, direction); // -> direction.addUnit(isForward, deque)
}
